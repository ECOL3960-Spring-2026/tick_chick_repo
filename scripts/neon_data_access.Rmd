---
title: "neon_data_access"
output: html_document
date: "2026-02-19"
---

## Import Packages

```{r load packages, message = FALSE}

#install.packages("tidyverse")
#install.packages("here")
#install.packages("neonUtilities")

library(tidyverse)
library(here)
library(neonUtilities)

```

## NEON Data Products, Sites, and Dates

```{r set NEON conditions}

# Tick Pathogen Status
tick_pathogens <- "DP1.10092.001"

# Relative Humidity
relative_humidity <- "DP1.00098.001"

# Tick Abundance - drag cloth
tick_abundance <- "DP1.10093.001"

# Assigning two sites
my_sites <- c("HARV","DELA")

# Start date (as YYYY-MM)
start_date <- "2016-01"

# End date (as YYYY-MM)
end_date <- "2023-12"

```

## Tick Pathogens Data Grab Code

```{r Pull Tick Pathogens, eval=FALSE}

# Pull Tick Pathogen data
list2env((loadByProduct(dpID = tick_pathogens,
                        site = my_sites,
                        include.provisional = FALSE,
                        startdate = start_date,
                        enddate = end_date)),
         .GlobalEnv)

```

## Tick Pathogen Status

```{r save tick pathogen csv, eval=FALSE}

# Variable names

variables_10092 %>% 
  write_csv(here("data", "raw", "tick_pathogen_variable.csv"))

# Tick Pathogen Y/N

tck_pathogen %>% 
  write_csv(here("data", "raw", "tick_pathogen_status.csv"))
```

## Tick Abundance Data Grab Code

```{r Pull Tick Abundance, eval=FALSE}

# Pull Tick Abundance data
list2env((loadByProduct(dpID = tick_abundance,
                        site = my_sites,
                        include.provisional = FALSE,
                        startdate = start_date,
                        enddate = end_date)),
         .GlobalEnv)

```

## Tick Abundance Status

```{r save tick abundance csv, eval=FALSE}

# Variable names

variables_10093 %>% 
  write_csv(here("data", "raw", "tick_abundance_variable.csv"))

# Tick Abundance Y/N

tck_fielddata %>% 
  write_csv(here("data", "raw", "tick_abundance_status.csv"))
```